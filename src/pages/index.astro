---
import Layout from '@layouts/main.astro';
import ProductList from '@components/ts/ProductList';
import { MockData } from '@models/item.ts'; 

const pageContent = {
  title: 'Bamazon'
};
---

<Layout content={pageContent}>
  <div class="w-full bg-gray-50 min-h-screen">
    <section class="bg-white py-16">
      <div class="mx-auto max-w-7xl px-8">
        <div class="text-center">
          <h1 class="text-4xl font-bold text-gray-900 mb-4">
            <span class="text-black">モバイルオーダー</span>
          </h1>
        </div>
      </div>
    </section>
    <section class="py-16">
      <div class="mx-auto max-w-7xl px-8">
        <div class="flex flex-col items-center pb-10 gap-y-7">
          <h2 class="text-3xl font-bold text-gray-900 text-center">
            商品一覧
          </h2>
        </div> 
        <ProductList client:load products={MockData} />
      </div>
    </section>
  </div>
</Layout>